import { GitCommit } from '../msg/git-commit';
import { Stories } from './stories';

export class Groups {
  public readonly groups = new Map<string, Stories>();
  public add(gc: GitCommit): Groups {
    if ()
    return this;
  }
}
